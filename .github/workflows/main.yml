name: Quality Gate CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test-user-service:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          cd user_service
          pip install -r requirements.txt
          pip install pytest pytest-cov
      - name: Initialize database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          PYTHONPATH: "${{ github.workspace }}:${{ github.workspace }}/user_service"
        run: |
          cd user_service
          python -c "from models import init_db; init_db()"
      - name: Run tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          PYTHONPATH: "${{ github.workspace }}:${{ github.workspace }}/user_service"
        run: |
          cd user_service
          pytest tests/ -v --cov=./ --cov-report=xml

  test-product-service:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          cd product_service
          pip install -r requirements.txt
          pip install pytest pytest-cov
      - name: Initialize database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          PYTHONPATH: "${{ github.workspace }}:${{ github.workspace }}/product_service"  # ✅ 修复
        run: |
          cd product_service
          python -c "from models import init_db; init_db()"
      - name: Run tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          PYTHONPATH: "${{ github.workspace }}:${{ github.workspace }}/product_service"
        run: |
          cd product_service
          pytest tests/ -v --cov=./ --cov-report=xml

  test-order-service:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          cd order_service
          pip install -r requirements.txt
          pip install pytest pytest-cov
      - name: Initialize database
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          PYTHONPATH: "${{ github.workspace }}:${{ github.workspace }}/order_service"  # ✅ 修复
        run: |
          cd order_service  # ✅ 修复
          python -c "from models import init_db; init_db()"
      - name: Run tests  # ✅ 新增
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          PYTHONPATH: "${{ github.workspace }}:${{ github.workspace }}/order_service"
        run: |
          cd order_service
          pytest tests/ -v --cov=./ --cov-report=xml